# ========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# LittleFalls - Sistema de Gestión Veterinaria
# ========================================

# ==================== BASE DE DATOS ====================
# URI de conexión a MongoDB
# Formato: mongodb://usuario:password@host:puerto/nombreBaseDatos
# Ejemplo local: mongodb://localhost:27017/littlefalls
# Ejemplo Atlas: mongodb+srv://usuario:password@cluster.mongodb.net/littlefalls
MONGODB_URI=mongodb://localhost:27017/littlefalls

# ==================== SERVIDOR ====================
# Puerto en el que correrá el servidor Express
PORT=3000

# ==================== JWT (JSON WEB TOKENS) ====================
# Clave secreta para firmar los Access Tokens
# IMPORTANTE: Cambiar en producción por una clave fuerte y única
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_clave_secreta_super_segura_cambiar_en_produccion

# Clave secreta para firmar los Refresh Tokens
# IMPORTANTE: Debe ser diferente al JWT_SECRET
JWT_REFRESH_SECRET=tu_refresh_secret_super_segura_cambiar_en_produccion

# ==================== EMAIL (NODEMAILER) ====================
# Servidor SMTP para envío de correos
# Gmail: smtp.gmail.com
EMAIL_HOST=smtp.gmail.com

# Puerto SMTP
# Gmail TLS: 587
# Gmail SSL: 465
EMAIL_PORT=587

# Usar conexión segura
# true para SSL (puerto 465)
# false para TLS (puerto 587)
EMAIL_SECURE=false

# Credenciales de correo
# IMPORTANTE: Para Gmail, usar una "Contraseña de aplicación"
# Instrucciones: https://support.google.com/accounts/answer/185833
EMAIL_USER=tu_correo@gmail.com
EMAIL_PASSWORD=tu_contraseña_de_aplicacion

# Nombre del remitente que aparecerá en los correos
EMAIL_FROM_NAME=LittleFalls Veterinaria

# ==================== ENTORNO ====================
# Entorno de ejecución: development, production, test
NODE_ENV=development

# ==================== INSTRUCCIONES ====================
# 1. Copiar este archivo y renombrarlo a .env
# 2. Completar todos los valores con tu configuración real
# 3. NUNCA subir el archivo .env al repositorio (está en .gitignore)
# 4. Para Gmail: Activar "Verificación en 2 pasos" y generar "Contraseña de aplicación"
# 5. Para MongoDB Atlas: Obtener la URI de conexión desde el panel de control

# ==================== NOTAS DE SEGURIDAD ====================
# - Cambiar JWT_SECRET y JWT_REFRESH_SECRET en producción
# - Usar contraseñas de aplicación para servicios de email
# - No compartir este archivo con credenciales reales
# - Rotar secretos periódicamente en producción
